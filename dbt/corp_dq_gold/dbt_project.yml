name: 'corp_dq_gold'
version: '1.0.0'
config-version: 2

profile: 'corp_dq_gold'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"

models:
  corp_dq_gold:
    marts:
      +materialized: table
    staging:
      +materialized: view
